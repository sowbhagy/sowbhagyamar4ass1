#Write a program to count the duplicate elements in the list.
MyList = ["1", "2", "1", "1", "4"]
count=0
for i in MyList:
    if i == '1': 
        count = count + 1  
print ("duplicate num is :", count)
